The `input()` function is designed to take input from the user, and whatever it takes from the user, it will automatically convert it into a string.

A variable is a method of storing data for reuse.

Numbers in Python:

* A whole number is called an integer.
* A decimal is considered a float, or a floating number ("float").
* Division will convert numbers to floating numbers, and the result will become a floating number.

